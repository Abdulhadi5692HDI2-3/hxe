// a.hxe - test

// functions
function runBinary(bin) {
    return sysrun(bin);
}
function waitSec(sec) {
    return syswait(sec);
}
var binary = "/usr/bin/gcc --version";

print "Running program " + binary + ". as a test!";
var result = runBinary(binary);
print "result code is:";
print result;